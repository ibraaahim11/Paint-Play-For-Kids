#include "Resize.h"
#include "DEFS.h"
#include "Figures/CFigure.h"



//void ResizeFeature::resizeSelectedFigure(CFigure* selectedFigure, double factor) {
    //if (selectedFigure) {
       
      //  int originalWidth = selectedFigure->GetGfxInfo().FigureDimensions.width;
        //int originalHeight = selectedFigure->GetGfxInfo().FigureDimensions.height;

       
        //int newWidth = static_cast<int>(originalWidth * factor);
        //int newHeight = static_cast<int>(originalHeight * factor);

        
        //GfxInfo newFigureGfxInfo = selectedFigure->GetGfxInfo();
        //newFigureGfxInfo.FigureDimensions.width = newWidth;
        //newFigureGfxInfo.FigureDimensions.height = newHeight;

        
        //selectedFigure->SetGfxInfo(newFigureGfxInfo);

        
        //selectedFigure->Draw(pOut); 
    //}
//}
